const photoUploadOptions = {
  name: "projectPhotos",
  maxCount: 10,
};

const videoUploadOptions = {
  name: "projectVideo",
  maxCount: 1,
};

const avatarUploadOptions = {
  name: "avatar",
  maxCount: 1,
};

export { photoUploadOptions, videoUploadOptions, avatarUploadOptions };
